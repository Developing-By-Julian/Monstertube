<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Configuratie Formulier</title>
    <style>
        body {
    font-family: Arial, sans-serif;
    margin: 0;
    padding: 0;
    background-color: #222;
    color: #fff;
}

.container {
    max-width: 800px;
    margin: 0 auto;
    padding: 20px;
}

h1 {
    margin-top: 0;
    color: #fff;
}

a {
    display: block;
    text-decoration: none;
    margin-bottom: 10px;
    padding: 8px;
    box-sizing: border-box;
    background-color: #444;
    color: #fff;
    border: 1px solid #666;
    width: 65px;
    left: 50px;
    position: relative;
}



form {
    margin-top: 20px;
}

input[type="text"],
input[type="email"],
input[type="password"],
textarea,
select {
    width: 100%;
    padding: 10px;
    margin: 8px 0;
    border: 1px solid #555;
    border-radius: 5px;
    box-sizing: border-box;
    background-color: #333;
    color: #ffffff;
    appearance: none; /* Verwijder de standaard dropdown-stijl */
}

/* Stijl voor opties */
select option {
    background-color: #333;
    color: #ffffff;
}

/* Stijl voor geselecteerde optie */
select option:checked {
    background-color: #ff7f50;
}

input[type="checkbox"],
input[type="radio"] {
    margin: 8px 8px 8px 0;
}

label {
    display: block;
    font-weight: bold;
    color: #ffffff;
}

/* Stijl voor de submit-knop */
.submit {
    background-color: #ff7f50;
    color: #ffffff;
    padding: 10px 20px;
    border: none;
    border-radius: 5px;
    cursor: pointer;
    transition: background-color 0.3s ease;
    margin-top: 20px;
    width: 100px;
}

.submit:hover {
    background-color: #ff6347;
}
.button {
    display: inline-block;
    padding: 10px 20px;
    margin: 10px;
    border-radius: 5px;
    text-decoration: none;
    color: #ffffff;
    background-color: #ff7f50;
    border: 2px solid #ff7f50;
    transition: background-color 0.3s ease, border-color 0.3s ease;
    width: 110px;

}

.button:hover {
    background-color: #ff6347;
    border-color: #ff6347;
}

.error-message {
    color: #dc3545;
}
</style>
</head>
<body>
    <h1>Configuratie Formulier</h1>
    <form action="/dashboard/create-reward" method="POST">      


        <select id="rol" name="role">
            <% data.roles.forEach(role => { %>
                <option value="<%= role.name %>"><%= role.name %></option>
            <% }) %>
        </select>
        <br>
        <label for="value">€:</label>
        <input type="text" id="value" name="value" required>

        <button type="submit" class="submit">Opslaan</button>
    </form>
    <a class="button" href="/dashboard?guildid=<%=data.session.guildid %>">Terug naar dashboard</a>

</body>
</html>
